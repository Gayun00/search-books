export interface BookProps {
  title: string;
  subtitle: string;
  image: string;
  url: string;
}

export interface BookData {
  title: string;
  subtitle: string;
  image: string;
  url: string;
  price: string;
  isbn13: string;
}
